<UserControl x:Class="ESD.PM.Views.FolderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        Height="450" Width="800">
    <Grid Background="#FFE1E5EC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock Text="{Binding Name}" HorizontalAlignment="Right"/>
            <TextBox Text="{Binding SelectedFolderName.FullName, Mode=OneWay}" 
                     HorizontalAlignment="Left" TextWrapping="Wrap" IsReadOnly="True" 
                     Height="50" Width="400" Background="White" Margin="0,50,0,0"/>
            <ItemsControl ItemsSource="{Binding Tags}" HorizontalAlignment="Left" 
                          Margin="0,5,0,0" VerticalAlignment="Top" Height="35" 
                          Width="399">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding State}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding FilteredDocsList}" 
                 HorizontalAlignment="Center" Width="400" 
                 SelectedItem="{Binding SelectedFolderName}" Margin="0,0,0,5">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding OpenCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListBox>
        <Button Content="TOGGLE VIEW" Width="100" Height="50" 
                HorizontalAlignment="Left" VerticalAlignment="Top" 
                Command="{Binding ToggleViewCommand}" 
                IsEnabled="{Binding ToggleViewCommandActive}"/>
        <Button Content="OPEN FOLDER" Width="100" Height="50" 
                HorizontalAlignment="Left" VerticalAlignment="Top" 
                Command="{Binding OpenFolderCommand}" Margin="100,0,0,0"/>
    </Grid>
</UserControl>
