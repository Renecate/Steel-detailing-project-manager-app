<Window x:Class="ESD.PM.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:ESD.PM.Views" xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="ShellView"     
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Width="1920"
        Height="1080"
        MaxHeight="1080"
        MinWidth="990"
        MinHeight="1000"
        WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="55"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="29"/>
            <RowDefinition Height="Auto" 
                       MinHeight="49"
                       MaxHeight="98"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Height="29" Grid.Column="1" Background="#FFEDEDED" MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="-" HorizontalAlignment="Left" VerticalAlignment="Top" Width="50" Height="29" Click="HideButton_OnClick"/>
            <Button Grid.Column="2" Content="+" HorizontalAlignment="Left" VerticalAlignment="Top" Width="50" Height="29" Click="ExpandButton_OnClick"/>
            <Button Grid.Column="3" Content="X" HorizontalAlignment="Left" VerticalAlignment="Top" Width="50" Height="29" Click="CloseButton_OnClick"/>
        </Grid>
        <WrapPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Width="930" Height="49">
                <ComboBox Width="350" Height="35" Margin="15,0,10,0" x:Name="ProjectsList" SelectedItem="{Binding SelectedProject}" FontSize="20">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <ComboBox Width="350" Height="35" Margin="10,0,10,0" x:Name="DisplayItemsList" SelectedItem="{Binding SelectedItem}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" FontSize="20" IsEnabled="False"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Button Content="CREATE PROJECT" Height="35" MinWidth="100" Margin="10,0,15,0" HorizontalContentAlignment="Center" Width="160" />

            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Height="49" Margin="5,0,0,0">
                <Button Content="STRUCTURALS" Width="160" Height="35" Margin="10,0,10,0"/>
                <Button Content="ARCHITECTURALS" Width="160" Height="35" Margin="10,0,10,0"/>
                <Button Content="+" Width="35" Height="35" Margin="10,0,10,0"/>
            </StackPanel>

        </WrapPanel>
        <ItemsControl Grid.Column="1" Grid.Row="2" x:Name="ItemsList">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Background="#FFE0F3F3" Width="450" Height="300" Margin="10,10,10,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="100"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Right">
                        </TextBlock>
                        <ListBox Grid.Row="1" ItemsSource="{Binding DocsList}" HorizontalAlignment="Left" Width="400" SelectedItem="{Binding SelectedDoc}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="MouseDoubleClick">
                                    <b:InvokeCommandAction
                                        Command="{Binding OpenCommand}"/>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </ListBox>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>



    </Grid>
</Window>
